# 🩺 Backend - Sistema de Salud y Bienestar

Este proyecto es el backend del sistema **Salud y Bienestar**, desarrollado con **Node.js**, **Express** y **MongoDB**.  
Incluye autenticación con **JWT**, encriptación de contraseñas con **bcrypt**, y estructura modular para controladores, modelos y rutas.

---

## 🚀 Requisitos previos

Antes de iniciar, asegúrate de tener instalado:

- [Node.js](https://nodejs.org/) (v16 o superior)
- [npm](https://www.npmjs.com/)
- [MongoDB](https://www.mongodb.com/) o una base de datos Mongo en la nube (por ejemplo, [MongoDB Atlas](https://www.mongodb.com/atlas))

---

## 📂 Estructura del proyecto

```bash
salud_y_bienestar_backend/
├── src/
│   ├── assets/
│   ├── common/
│   ├── config/
│   ├── controllers/
│   ├── middlewares/
│   ├── models/
│   └── routes/
├── .env
├── .gitignore
├── package.json
└── server.js
```


Antes de iniciar, asegúrate de tener instalado:

- [Node.js](https://nodejs.org/) (v22 o superior)
- [npm](https://www.npmjs.com/)
- [MongoDB](https://www.mongodb.com/) o una base de datos Mongo en la nube (por ejemplo, [MongoDB Atlas](https://www.mongodb.com/atlas))

---

## Instalación
1. Clona este repositorio:
   ```bash
   git clone

2. Navega al directorio del proyecto:
   ```bash
   cd salud_bienestar_backend_app
   ```
3. Instala las dependencias:
   ```bash
    npm install

4. Copia el archivo `.env.example` a `.env` y configura las variables de entorno según tu entorno.

5. Inicia el servidor:
   ```bash
   npm start
   ```

